---
phase: 05-hooks-ui
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - src/features/infographic-generator/components/InfographicPreview.tsx
  - src/features/infographic-generator/components/ShareSheet.tsx
  - src/features/infographic-generator/components/BatchDayView.tsx
autonomous: true
requirements: [INFOG-022]

must_haves:
  truths:
    - "Preview supports pinch-to-zoom on mobile"
    - "Share sheet offers SMS, email, and link sharing"
    - "Batch day view displays swipeable card stack of results"
    - "All components are mobile-first"
  artifacts:
    - path: "src/features/infographic-generator/components/InfographicPreview.tsx"
      provides: "Pinch-to-zoom infographic image viewer"
      exports: ["InfographicPreview"]
    - path: "src/features/infographic-generator/components/ShareSheet.tsx"
      provides: "Share options for SMS, email, and link"
      exports: ["ShareSheet"]
    - path: "src/features/infographic-generator/components/BatchDayView.tsx"
      provides: "Swipeable card stack for batch results"
      exports: ["BatchDayView"]
  key_links:
    - from: "InfographicPreview"
      to: "InfographicResponse"
      via: "type import"
      pattern: "import.*InfographicResponse"
    - from: "BatchDayView"
      to: "useInfographicBatch"
      via: "hook import"
      pattern: "import.*useInfographicBatch"
---

<objective>
Create the preview component (pinch-to-zoom), share sheet (SMS/email/link), and batch day view (swipeable card stack).

Purpose: These complete the generation experience — reps see their result, share it, and review batch outputs.
Output: 3 component files + update barrel export
</objective>

<execution_context>
@C:/Users/scott/Documents/guardian-intel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/scott/Documents/guardian-intel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From types:
```typescript
export interface InfographicResponse { imageData: string, imageUrl?: string, model: string, chainUsed: boolean, generationTimeMs: number, cached: boolean }
```

From hooks:
```typescript
useInfographicBatch(): { isBatching, batchProgress: BatchCustomerStatus[], startBatch(customerIds), cancelBatch() }
// BatchCustomerStatus: { customerId, status, result?, error? }
```

Pattern reference:
- DeckPreview.tsx uses img tags with full-width rendering and scroll
- DeckGeneratorModal has share URL copy: navigator.clipboard.writeText(shareUrl)
- File downloads via file-saver library: saveAs(blob, filename)
- Native Web Share API: navigator.share({ title, text, url })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: InfographicPreview with pinch-to-zoom</name>
  <files>
    src/features/infographic-generator/components/InfographicPreview.tsx
  </files>
  <read_first>
    src/features/deck-generator/components/DeckPreview.tsx
    src/features/infographic-generator/types/infographic.types.ts
  </read_first>
  <action>
Create InfographicPreview in src/features/infographic-generator/components/InfographicPreview.tsx:

1. "use client" directive.
2. Import React, useState, useRef, useCallback from React; motion from framer-motion; ZoomIn, ZoomOut, Download, Share2, RotateCcw from lucide-react.
3. Props: { imageData: string, imageUrl?: string, onShare?: () => void, onDownload?: () => void }
4. Zoom state: scale (number, default 1), position (x, y for panning).
5. Touch gesture handling for pinch-to-zoom:
   - Track two-finger touches via onTouchStart, onTouchMove, onTouchEnd
   - Calculate distance between touches to determine scale delta
   - useRef for initial touch distance and current scale
   - Clamp scale between 0.5 and 3
6. Desktop zoom controls:
   - Toolbar overlay at bottom: flex row with icon buttons
   - Zoom In (+), Zoom Out (-), Reset (1x), Download, Share
   - Button styling: bg-surface-primary/90 backdrop-blur-sm rounded-full p-2
7. Image display:
   - Container: relative overflow-hidden rounded-lg bg-surface-secondary, aspect-auto
   - Image: motion.img with animate={{ scale, x: position.x, y: position.y }}
   - src: `data:image/png;base64,${imageData}` or imageUrl if available
   - Double-tap to toggle between 1x and 2x zoom
8. Download handler:
   - Convert base64 to blob, create download link
   - Filename: "briefing-{timestamp}.png"
  </action>
  <verify>
    <automated>cd C:/Users/scott/Documents/guardian-intel && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "use client" src/features/infographic-generator/components/InfographicPreview.tsx
    - grep -q "onTouchStart\|onTouchMove\|touch" src/features/infographic-generator/components/InfographicPreview.tsx
    - grep -q "scale" src/features/infographic-generator/components/InfographicPreview.tsx
    - grep -q "Download\|onDownload" src/features/infographic-generator/components/InfographicPreview.tsx
    - grep -q "base64\|imageData" src/features/infographic-generator/components/InfographicPreview.tsx
  </acceptance_criteria>
  <done>InfographicPreview supports pinch-to-zoom on mobile with desktop zoom controls and download.</done>
</task>

<task type="auto">
  <name>Task 2: ShareSheet and BatchDayView</name>
  <files>
    src/features/infographic-generator/components/ShareSheet.tsx
    src/features/infographic-generator/components/BatchDayView.tsx
  </files>
  <read_first>
    src/features/deck-generator/components/DeckGeneratorModal.tsx
    src/features/infographic-generator/hooks/useInfographicBatch.ts
    src/features/infographic-generator/components/InfographicPreview.tsx
  </read_first>
  <action>
Create ShareSheet in src/features/infographic-generator/components/ShareSheet.tsx:

1. "use client" directive.
2. Import motion, AnimatePresence from framer-motion; MessageSquare, Mail, Link, Check, X from lucide-react.
3. Props: { isOpen: boolean, onClose: () => void, imageData: string, imageUrl?: string, customerName?: string }
4. Bottom sheet pattern:
   - AnimatePresence wrapper
   - Backdrop: motion.div fixed inset-0, onClick={onClose}
   - Sheet: motion.div fixed bottom-0 left-0 right-0, slide up (y: "100%" → 0), rounded-t-2xl bg-surface-primary, max-h-[50vh]
   - Drag handle bar at top (w-10 h-1 rounded-full bg-surface-secondary mx-auto)
5. Three share options as large touchable rows:
   a. SMS: MessageSquare icon, "Send via SMS" label
      - onClick: opens sms: URL with body text (or navigator.share if available)
   b. Email: Mail icon, "Send via Email" label
      - onClick: opens mailto: with subject "Your Property Briefing" and body
   c. Copy Link: Link icon, "Copy Link" label
      - onClick: if imageUrl, copy to clipboard via navigator.clipboard.writeText
      - Show Check icon briefly after copy (copied state, reset after 2s)
6. Native share fallback: if navigator.share is available, show a "More options..." button that triggers native share with the image URL.
7. Close button (X) at top right.

Create BatchDayView in src/features/infographic-generator/components/BatchDayView.tsx:

1. "use client" directive.
2. Import React, useState from React; motion, AnimatePresence, useMotionValue, useTransform from framer-motion; ChevronLeft, ChevronRight, CheckCircle2, Loader2, AlertCircle from lucide-react.
3. Props: { batchProgress: Array<{ customerId: string, customerName?: string, status: string, result?: InfographicResponse, error?: string }> }
4. Card stack layout:
   - State: activeIndex (number, default 0)
   - Show current card full size with previous/next cards peeking behind (smaller scale, offset)
   - Swipe gestures: motion.div with drag="x", onDragEnd to advance/retreat
   - Drag constraints: dragConstraints={{ left: 0, right: 0 }}, dragElastic: 0.2
   - If drag velocity/offset exceeds threshold, change activeIndex
5. Card content per customer:
   - Status === 'pending': gray card with customer name and "Waiting..." text
   - Status === 'generating': card with Loader2 spinning and "Generating..." text
   - Status === 'complete': show result image (mini preview), customer name, CheckCircle2 badge
   - Status === 'error': red card with error message and AlertCircle
6. Navigation:
   - Left/Right chevron buttons (desktop)
   - Dot indicators at bottom: filled dot for active, outline for others
   - Progress summary text: "3 of 8 ready" showing completed count
7. Tap on completed card opens full InfographicPreview (communicate via onSelectResult callback prop).

Update barrel export in src/features/infographic-generator/components/index.ts to add:
```typescript
export { GenerationProgress } from './GenerationProgress';
export { ConversationalInput } from './ConversationalInput';
export { InfographicPreview } from './InfographicPreview';
export { ShareSheet } from './ShareSheet';
export { BatchDayView } from './BatchDayView';
```
  </action>
  <verify>
    <automated>cd C:/Users/scott/Documents/guardian-intel && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "use client" src/features/infographic-generator/components/ShareSheet.tsx
    - grep -q "SMS\|sms\|MessageSquare" src/features/infographic-generator/components/ShareSheet.tsx
    - grep -q "mailto\|Mail\|Email" src/features/infographic-generator/components/ShareSheet.tsx
    - grep -q "clipboard\|Copy Link" src/features/infographic-generator/components/ShareSheet.tsx
    - grep -q "use client" src/features/infographic-generator/components/BatchDayView.tsx
    - grep -q "drag\|swipe\|onDrag" src/features/infographic-generator/components/BatchDayView.tsx
    - grep -q "batchProgress" src/features/infographic-generator/components/BatchDayView.tsx
    - grep -q "activeIndex" src/features/infographic-generator/components/BatchDayView.tsx
    - grep -q "InfographicPreview" src/features/infographic-generator/components/index.ts
    - grep -q "ShareSheet" src/features/infographic-generator/components/index.ts
    - grep -q "BatchDayView" src/features/infographic-generator/components/index.ts
  </acceptance_criteria>
  <done>ShareSheet provides SMS/email/link sharing as bottom sheet. BatchDayView shows swipeable card stack with progressive status. Barrel export updated with all 6 components.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` compiles without errors
- All 3 component files exist
- ShareSheet offers 3 sharing methods (SMS, email, link)
- BatchDayView supports swipe navigation
- InfographicPreview supports pinch-to-zoom
- Barrel export includes all 6 components
</verification>

<success_criteria>
- InfographicPreview supports pinch-to-zoom and download
- ShareSheet offers SMS, email, and link sharing as bottom sheet
- BatchDayView shows swipeable card stack with progressive batch results
- All components are mobile-first
</success_criteria>

<output>
After completion, create `.planning/phases/05-hooks-ui/05-04-SUMMARY.md`
</output>
