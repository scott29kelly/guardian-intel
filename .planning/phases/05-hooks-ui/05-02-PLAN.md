---
phase: 05-hooks-ui
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/features/infographic-generator/components/InfographicGeneratorModal.tsx
  - src/features/infographic-generator/components/PresetSelector.tsx
  - src/features/infographic-generator/components/TopicPicker.tsx
  - src/features/infographic-generator/components/index.ts
autonomous: true
requirements: [INFOG-019, INFOG-020]

must_haves:
  truths:
    - "Generator modal has three tabs: Presets, Custom, Ask AI"
    - "Modal renders as mobile-first bottom sheet with AnimatePresence"
    - "Single Generate button triggers generation regardless of active tab"
    - "Preset selector shows visual cards grouped by usage moment"
    - "Topic picker has checkbox grid with web search badges and audience toggle"
    - "No model names, quality sliders, or resolution selectors visible"
  artifacts:
    - path: "src/features/infographic-generator/components/InfographicGeneratorModal.tsx"
      provides: "Three-mode generator modal orchestrator"
      exports: ["InfographicGeneratorModal"]
    - path: "src/features/infographic-generator/components/PresetSelector.tsx"
      provides: "Visual preset card grid with moment labels"
      exports: ["PresetSelector"]
    - path: "src/features/infographic-generator/components/TopicPicker.tsx"
      provides: "Checkbox topic grid with audience toggle"
      exports: ["TopicPicker"]
    - path: "src/features/infographic-generator/components/index.ts"
      provides: "Barrel export for all components"
  key_links:
    - from: "InfographicGeneratorModal"
      to: "useInfographicGeneration"
      via: "hook import"
      pattern: "import.*useInfographicGeneration"
    - from: "PresetSelector"
      to: "useInfographicPresets"
      via: "hook import"
      pattern: "import.*useInfographicPresets"
---

<objective>
Create the generator modal with three-mode tabs (Presets | Custom | Ask AI) and its two child components: PresetSelector (visual cards) and TopicPicker (checkbox grid with audience toggle).

Purpose: This is the primary entry point for reps to generate infographics. It orchestrates tab selection and delegates to the correct input mode.
Output: 4 files — 3 components + barrel export
</objective>

<execution_context>
@C:/Users/scott/Documents/guardian-intel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/scott/Documents/guardian-intel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From src/features/infographic-generator/types/infographic.types.ts:
```typescript
export type GenerationMode = 'preset' | 'custom' | 'conversational';
export type InfographicAudience = 'internal' | 'customer-facing';
export type UsageMoment = 'Parking Lot' | 'Post-Storm' | 'Meeting Prep' | 'Leave-Behind' | 'Batch';
export interface InfographicPreset { id, name, description, audience, modules: TopicModuleConfig[], usageMoment, icon }
export interface TopicModule { id, label, dataSource, requiresWebSearch, visualElement }
export interface TopicModuleConfig { module: TopicModule, enabled: boolean, required: boolean }
export interface InfographicRequest { customerId, mode, presetId?, selectedModules?, conversationalPrompt?, audience? }
```

From hooks (Plan 01 output):
```typescript
export function useInfographicGeneration(): { isGenerating, progress, result, error, generate(req), reset() }
export function useInfographicPresets(): { presets, getPreset, getByMoment, searchPresets }
```

Pattern reference (deck-generator):
- src/features/deck-generator/components/DeckGeneratorModal.tsx — step-based modal with AnimatePresence, fixed inset-0 backdrop
- src/features/deck-generator/components/DeckTemplateSelector.tsx — grid with search + staggered animations
- src/features/deck-generator/components/DeckCustomizer.tsx — section toggles with checkbox indicators

UI primitives:
- src/components/ui/button.tsx — cva variants (default, outline, ghost, secondary)
- src/components/ui/badge.tsx — variants (default, secondary, success, accent)
- src/components/ui/card.tsx — compound component (Card, CardHeader, CardContent)
- Icons from lucide-react
- Framer motion for all animations
- Tailwind classes: bg-surface-primary, text-text-primary, border-border, bg-accent-primary
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: InfographicGeneratorModal and PresetSelector</name>
  <files>
    src/features/infographic-generator/components/InfographicGeneratorModal.tsx
    src/features/infographic-generator/components/PresetSelector.tsx
  </files>
  <read_first>
    src/features/deck-generator/components/DeckGeneratorModal.tsx
    src/features/deck-generator/components/DeckTemplateSelector.tsx
    src/features/infographic-generator/types/infographic.types.ts
    src/features/infographic-generator/hooks/useInfographicGeneration.ts
    src/features/infographic-generator/hooks/useInfographicPresets.ts
    src/components/ui/button.tsx
    src/components/ui/badge.tsx
  </read_first>
  <action>
Create InfographicGeneratorModal in src/features/infographic-generator/components/InfographicGeneratorModal.tsx:

1. Add "use client" directive.
2. Import React, useState, useCallback from React; motion, AnimatePresence from framer-motion; X, Zap from lucide-react.
3. Import useInfographicGeneration from "../hooks"; import types.
4. Props: { isOpen: boolean, onClose: () => void, customerId: string, customerName?: string }
5. Modal structure following DeckGeneratorModal pattern:
   - AnimatePresence wrapping everything
   - Backdrop: motion.div fixed inset-0 bg-black/60 backdrop-blur-sm z-[9999]
   - Content: motion.div with spring animation (scale 0.95 → 1), max-w-lg w-full max-h-[90vh]
   - On mobile (< md): position at bottom with rounded-t-2xl, slide up animation (y: "100%" → 0) instead of scale
6. State: activeTab (GenerationMode, default 'preset'), selectedPresetId, selectedModules, conversationalPrompt, audience.
7. Three tab buttons at top: "Presets" | "Custom" | "Ask AI" — active tab gets accent styling.
8. Tab content area with AnimatePresence mode="wait":
   - 'preset' tab: renders <PresetSelector> component
   - 'custom' tab: renders <TopicPicker> component (Plan 02 Task 2)
   - 'conversational' tab: renders <ConversationalInput> component (Plan 03 Task 2, placeholder div for now)
9. Footer with single "Generate Briefing" button (Zap icon):
   - Disabled when: no preset selected (preset mode), no modules selected (custom mode), empty prompt (conversational mode).
   - onClick calls generate() from useInfographicGeneration with appropriate InfographicRequest based on active tab.
   - When isGenerating, button shows spinner and "Generating..." text.
10. When result is available, modal transitions to show GenerationProgress component (Plan 03, pass as children or step).
11. CRITICAL: No model names, quality sliders, or configuration exposed anywhere.

Create PresetSelector in src/features/infographic-generator/components/PresetSelector.tsx:

1. "use client" directive.
2. Import useInfographicPresets from "../hooks".
3. Props: { selectedPresetId: string | null, onSelect: (presetId: string) => void }
4. Use useInfographicPresets() to get presets and search.
5. Search input at top (optional, simple text filter).
6. Group presets by usageMoment using a Map — render grouped sections.
7. Each preset as a motion.button card:
   - Icon (map preset.icon string to lucide-react icon component using a lookup object similar to DeckTemplateSelector)
   - Name (bold), description (text-muted)
   - Badge showing audience ("Internal" blue, "Customer" gold)
   - Badge showing usageMoment
   - Selected state: ring-2 ring-accent-primary, scale slightly larger
   - Staggered animation: delay: index * 0.05
8. Grid layout: grid-cols-1 sm:grid-cols-2 gap-3
  </action>
  <verify>
    <automated>cd C:/Users/scott/Documents/guardian-intel && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "use client" src/features/infographic-generator/components/InfographicGeneratorModal.tsx
    - grep -q "AnimatePresence" src/features/infographic-generator/components/InfographicGeneratorModal.tsx
    - grep -q "Presets.*Custom.*Ask AI" src/features/infographic-generator/components/InfographicGeneratorModal.tsx
    - grep -q "Generate Briefing" src/features/infographic-generator/components/InfographicGeneratorModal.tsx
    - grep -q "useInfographicGeneration" src/features/infographic-generator/components/InfographicGeneratorModal.tsx
    - grep -qv "NB2\|NB Pro\|nano.*banana\|gemini\|quality.*slider\|resolution.*select" src/features/infographic-generator/components/InfographicGeneratorModal.tsx
    - grep -q "use client" src/features/infographic-generator/components/PresetSelector.tsx
    - grep -q "useInfographicPresets" src/features/infographic-generator/components/PresetSelector.tsx
    - grep -q "usageMoment" src/features/infographic-generator/components/PresetSelector.tsx
    - grep -q "onSelect" src/features/infographic-generator/components/PresetSelector.tsx
  </acceptance_criteria>
  <done>InfographicGeneratorModal provides three-tab interface with Generate button. PresetSelector shows grouped visual cards with audience badges.</done>
</task>

<task type="auto">
  <name>Task 2: TopicPicker and barrel export</name>
  <files>
    src/features/infographic-generator/components/TopicPicker.tsx
    src/features/infographic-generator/components/index.ts
  </files>
  <read_first>
    src/features/deck-generator/components/DeckCustomizer.tsx
    src/features/infographic-generator/types/infographic.types.ts
    src/features/infographic-generator/templates/index.ts
    src/features/infographic-generator/components/InfographicGeneratorModal.tsx
  </read_first>
  <action>
Create TopicPicker in src/features/infographic-generator/components/TopicPicker.tsx:

1. "use client" directive.
2. Import types: TopicModule, TopicModuleConfig, InfographicAudience from types.
3. Props: { selectedModules: TopicModule[], onModulesChange: (modules: TopicModule[]) => void, audience: InfographicAudience, onAudienceChange: (audience: InfographicAudience) => void }
4. Get all available modules from all presets (deduplicated by module.id) using infographicPresets from templates/index.
5. Audience toggle at top:
   - Two-button segmented control: "Internal" | "Customer-Facing"
   - Internal = default, Customer-Facing = triggers NB Pro (but don't say that)
   - Label: "Who will see this?"
6. Module grid below:
   - Checkbox-style cards in grid-cols-2 sm:grid-cols-3 gap-2
   - Each card shows: module.label (bold), module.visualElement (muted text), web search badge if requiresWebSearch (Globe icon + "Web-enhanced" badge)
   - Selected state: bg-accent-primary/10 border-accent-primary ring-1
   - Click toggles module in/out of selectedModules array
   - motion.button with staggered delay
7. "Select All" / "Clear" buttons above the grid.

Create barrel export in src/features/infographic-generator/components/index.ts:
```typescript
export { InfographicGeneratorModal } from './InfographicGeneratorModal';
export { PresetSelector } from './PresetSelector';
export { TopicPicker } from './TopicPicker';
```
  </action>
  <verify>
    <automated>cd C:/Users/scott/Documents/guardian-intel && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "use client" src/features/infographic-generator/components/TopicPicker.tsx
    - grep -q "selectedModules" src/features/infographic-generator/components/TopicPicker.tsx
    - grep -q "audience" src/features/infographic-generator/components/TopicPicker.tsx
    - grep -q "requiresWebSearch\|Web-enhanced" src/features/infographic-generator/components/TopicPicker.tsx
    - grep -q "Who will see this" src/features/infographic-generator/components/TopicPicker.tsx
    - grep -q "InfographicGeneratorModal" src/features/infographic-generator/components/index.ts
    - grep -q "PresetSelector" src/features/infographic-generator/components/index.ts
    - grep -q "TopicPicker" src/features/infographic-generator/components/index.ts
  </acceptance_criteria>
  <done>TopicPicker provides checkbox module grid with audience toggle and web search badges. Barrel export re-exports all 3 components.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` compiles without errors
- All 4 component files exist under src/features/infographic-generator/components/
- No model names (NB2, NB Pro, Nano Banana, gemini) appear in any component file
- No quality sliders or resolution selectors in UI
- All components are "use client" components
- Barrel export re-exports all 3 components
</verification>

<success_criteria>
- Generator modal presents three tabs and a single Generate button
- Preset selector shows grouped visual cards with usage moment badges
- Topic picker provides checkbox grid with web search badges and audience toggle
- Zero model configuration exposed to the rep
</success_criteria>

<output>
After completion, create `.planning/phases/05-hooks-ui/05-02-SUMMARY.md`
</output>
