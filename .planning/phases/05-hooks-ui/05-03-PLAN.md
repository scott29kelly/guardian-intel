---
phase: 05-hooks-ui
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/features/infographic-generator/components/GenerationProgress.tsx
  - src/features/infographic-generator/components/ConversationalInput.tsx
autonomous: true
requirements: [INFOG-021, INFOG-023]

must_haves:
  truths:
    - "Progress bar is animated and non-linear (jumps at phase transitions)"
    - "Status messages are contextual with NO time estimates and NO model names"
    - "Background generation support shows 'generating in background' state"
    - "Conversational input has text field and suggested topic chips"
    - "Topic chips map to known TopicModules"
  artifacts:
    - path: "src/features/infographic-generator/components/GenerationProgress.tsx"
      provides: "Animated progress with contextual status messages"
      exports: ["GenerationProgress"]
    - path: "src/features/infographic-generator/components/ConversationalInput.tsx"
      provides: "Natural language input with topic chip suggestions"
      exports: ["ConversationalInput"]
  key_links:
    - from: "GenerationProgress"
      to: "GenerationProgress type"
      via: "type import"
      pattern: "import.*GenerationProgress.*types"
    - from: "ConversationalInput"
      to: "/api/ai/parse-infographic-intent"
      via: "referenced by parent modal (not called directly)"
---

<objective>
Create the generation progress display (animated non-linear progress bar with contextual messages) and conversational input component (natural language with topic chips).

Purpose: GenerationProgress provides visual feedback during generation. ConversationalInput enables the "Ask AI" tab.
Output: 2 component files
</objective>

<execution_context>
@C:/Users/scott/Documents/guardian-intel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/scott/Documents/guardian-intel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From types:
```typescript
export type GenerationPhase = 'data' | 'scoring' | 'generating' | 'refining' | 'complete';
export interface GenerationProgress { phase: GenerationPhase, percent: number, statusMessage: string }
export interface TopicModule { id, label, dataSource, requiresWebSearch, visualElement }
```

From hooks:
```typescript
useInfographicGeneration(): { isGenerating, progress: GenerationProgress | null, result, error, generate(), reset() }
```

Pattern reference:
- DeckGeneratorModal.tsx has inline progress bar with motion.div width animation
- Progress bar: motion.div with animate={{ width: `${progress}%` }}
- Branding: Navy (#1E3A5F), Gold (#D4A656), Teal (#4A90A4)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GenerationProgress component</name>
  <files>
    src/features/infographic-generator/components/GenerationProgress.tsx
  </files>
  <read_first>
    src/features/deck-generator/components/DeckGeneratorModal.tsx
    src/features/infographic-generator/types/infographic.types.ts
    src/features/infographic-generator/hooks/useInfographicGeneration.ts
  </read_first>
  <action>
Create GenerationProgress in src/features/infographic-generator/components/GenerationProgress.tsx:

1. "use client" directive.
2. Import motion, AnimatePresence from framer-motion; GenerationProgress type from types; Loader2, CheckCircle2, Sparkles from lucide-react.
3. Props: { progress: GenerationProgress | null, isGenerating: boolean, error: string | null, onBackgroundDismiss?: () => void }
4. Phase icon mapping: data → database icon, scoring → sparkles, generating → loader spinning, refining → wand, complete → check circle.
5. Main layout:
   - Centered column with padding
   - Animated phase icon at top (scale in on change, AnimatePresence mode="wait")
   - Status message below icon (text-lg font-medium text-text-primary)
   - Progress bar below message:
     * Container: h-2 w-full rounded-full bg-surface-secondary overflow-hidden
     * Fill: motion.div with gradient bg (navy to teal), animate width to progress.percent
     * NON-LINEAR: use spring transition with stiffness: 50, damping: 15 for organic feel
     * On phase transitions, add slight pause then jump (handled naturally by the hook's progress updates)
   - Percent text below bar (text-sm text-text-muted)
6. Complete state: CheckCircle2 with scale-in animation, "Your briefing is ready!" message, green accent.
7. Error state: red alert with error message and "Try Again" suggestion.
8. Background mode: if onBackgroundDismiss provided, show "Continue working — we'll notify you when it's ready" link that calls onBackgroundDismiss.
9. CRITICAL: No time estimates ("ETA: 30s"), no model names, no technical details.
  </action>
  <verify>
    <automated>cd C:/Users/scott/Documents/guardian-intel && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "use client" src/features/infographic-generator/components/GenerationProgress.tsx
    - grep -q "motion" src/features/infographic-generator/components/GenerationProgress.tsx
    - grep -q "statusMessage" src/features/infographic-generator/components/GenerationProgress.tsx
    - grep -q "onBackgroundDismiss" src/features/infographic-generator/components/GenerationProgress.tsx
    - grep -qv "ETA\|estimate\|seconds remaining\|NB2\|NB Pro\|gemini" src/features/infographic-generator/components/GenerationProgress.tsx
  </acceptance_criteria>
  <done>GenerationProgress shows animated non-linear progress with contextual messages, phase icons, and background generation dismiss option.</done>
</task>

<task type="auto">
  <name>Task 2: ConversationalInput component</name>
  <files>
    src/features/infographic-generator/components/ConversationalInput.tsx
  </files>
  <read_first>
    src/features/infographic-generator/types/infographic.types.ts
    src/features/infographic-generator/templates/index.ts
  </read_first>
  <action>
Create ConversationalInput in src/features/infographic-generator/components/ConversationalInput.tsx:

1. "use client" directive.
2. Import useState from React; motion from framer-motion; MessageSquare, Sparkles from lucide-react.
3. Props: { prompt: string, onPromptChange: (prompt: string) => void }
4. Layout:
   - Header text: "Describe what you need" (text-sm font-medium text-text-secondary)
   - Textarea input:
     * Placeholder: "e.g., Prep me for the Johnson meeting tomorrow"
     * Styled: bg-surface-secondary border border-border rounded-lg p-3 resize-none h-24
     * Focus: ring-2 ring-accent-primary
     * Controlled: value={prompt}, onChange
   - Suggested topic chips below:
     * Array of pre-defined suggestions:
       - "Prep me for my next appointment"
       - "Storm damage summary for this area"
       - "Customer leave-behind for a meeting"
       - "Insurance deadline overview"
       - "Competitive analysis for this neighborhood"
     * Each chip: motion.button with pill styling (rounded-full px-3 py-1.5 text-sm bg-surface-secondary border border-border hover:bg-surface-hover)
     * onClick sets the prompt text to the suggestion
     * Flex wrap layout with gap-2
     * Staggered animation: delay: index * 0.05
   - Helper text: "Our AI will select the best topics and format for your request" (text-xs text-text-muted)
5. No model names or technical details in any text.
  </action>
  <verify>
    <automated>cd C:/Users/scott/Documents/guardian-intel && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "use client" src/features/infographic-generator/components/ConversationalInput.tsx
    - grep -q "Describe what you need" src/features/infographic-generator/components/ConversationalInput.tsx
    - grep -q "Prep me for" src/features/infographic-generator/components/ConversationalInput.tsx
    - grep -q "onPromptChange" src/features/infographic-generator/components/ConversationalInput.tsx
    - grep -qv "NB2\|NB Pro\|gemini\|model" src/features/infographic-generator/components/ConversationalInput.tsx
  </acceptance_criteria>
  <done>ConversationalInput provides natural language textarea with 5 suggested topic chips.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` compiles without errors
- Both component files exist
- No model names, time estimates, or technical jargon in either component
- GenerationProgress shows animated progress with phase transitions
- ConversationalInput has topic chip suggestions
</verification>

<success_criteria>
- GenerationProgress provides animated non-linear progress bar with contextual messages and background dismiss
- ConversationalInput provides text input with 5 suggested topic chips
- Zero model configuration or time estimates exposed
</success_criteria>

<output>
After completion, create `.planning/phases/05-hooks-ui/05-03-SUMMARY.md`
</output>
