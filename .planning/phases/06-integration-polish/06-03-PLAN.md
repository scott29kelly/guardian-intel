---
phase: 06-integration-polish
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - src/features/infographic-generator/services/__tests__/modelIntelligence.test.ts
  - src/features/infographic-generator/utils/__tests__/promptComposer.test.ts
  - src/features/infographic-generator/utils/__tests__/intentParser.test.ts
  - tests/e2e/infographic-generation.spec.ts
autonomous: true
requirements: [INFOG-028, INFOG-029]

must_haves:
  truths:
    - "Unit tests cover model scoring, data assembler, prompt templates, and intent classification"
    - "E2E tests cover generation flows and sharing"
    - "Tests use vitest for unit tests and playwright for E2E"
    - "Tests follow existing patterns from tests/setup.ts"
  artifacts:
    - path: "src/features/infographic-generator/services/__tests__/modelIntelligence.test.ts"
      provides: "Unit tests for model scoring and selection"
      exports: []
    - path: "src/features/infographic-generator/utils/__tests__/promptComposer.test.ts"
      provides: "Unit tests for prompt template generation"
      exports: []
    - path: "src/features/infographic-generator/utils/__tests__/intentParser.test.ts"
      provides: "Unit tests for intent classification"
      exports: []
    - path: "tests/e2e/infographic-generation.spec.ts"
      provides: "E2E tests for generation flows"
      exports: []
---

<objective>
Create unit tests (Vitest) for model intelligence, prompt composer, and intent parser, plus E2E tests (Playwright) for generation flows.

Purpose: Test coverage for core business logic and user-facing flows.
Output: 4 test files
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
Test setup (tests/setup.ts):
- Mocks Next.js router, next-auth useSession
- Mock env vars available
- vitest globals enabled (describe, it, expect, vi)

Existing test reference:
- src/features/infographic-generator/utils/__tests__/infographicDataAssembler.test.ts
- Uses vi.fn(), vi.stubGlobal(), describe/it/expect pattern

Vitest config (vitest.config.ts):
- Environment: jsdom
- Setup: tests/setup.ts
- Globals: true

Playwright config (playwright.config.ts):
- Base URL, browser configs, etc.

Files to test:
- src/features/infographic-generator/services/modelIntelligence.ts — scoreRequest(), getModelForTask()
- src/features/infographic-generator/utils/promptComposer.ts — composePrompt() with model-specific templates
- src/features/infographic-generator/utils/intentParser.ts — parseIntent() classifying natural language
- src/features/infographic-generator/utils/infographicDataAssembler.ts — already has tests, reference pattern
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for model intelligence and prompt composer</name>
  <files>
    src/features/infographic-generator/services/__tests__/modelIntelligence.test.ts
    src/features/infographic-generator/utils/__tests__/promptComposer.test.ts
  </files>
  <read_first>
    src/features/infographic-generator/services/modelIntelligence.ts
    src/features/infographic-generator/utils/promptComposer.ts
    src/features/infographic-generator/utils/__tests__/infographicDataAssembler.test.ts
    src/features/infographic-generator/types/infographic.types.ts
    vitest.config.ts
    tests/setup.ts
  </read_first>
  <action>
Create src/features/infographic-generator/services/__tests__/modelIntelligence.test.ts:

1. Import describe, it, expect from vitest (or use globals).
2. Import the scoring/selection functions from modelIntelligence.
3. Import types for test data construction.
4. Test suites:
   - "scoreRequest": Tests scoring dimensions
     - Internal audience with simple modules → low audience score, low complexity
     - Customer-facing with many modules → high audience score, high complexity
     - Modules requiring web search → high webSearch score
   - "model selection": Tests model routing
     - Internal simple request → selects NB2 (lower cost)
     - Customer-facing request → selects NB Pro (higher fidelity)
     - Web search required → selects model with webSearchGrounding
     - Complex customer-facing → triggers chain strategy
   - "chain strategies": Tests chain selection
     - Chain A (web-grounded quality) triggered by customer-facing + web search
     - Chain B (complexity upgrade) triggered by high module count
5. Use realistic test data matching the type interfaces.
6. Follow exact pattern from infographicDataAssembler.test.ts.

Create src/features/infographic-generator/utils/__tests__/promptComposer.test.ts:

1. Import composePrompt or relevant functions from promptComposer.
2. Test suites:
   - "NB2 prompts": Verify search_types included, correct structure
   - "NB Pro prompts": Verify composition emphasis
   - "chain prompts": Verify split between data accuracy and refinement steps
   - "prompt content": Verify customer data interpolation, module inclusion
   - "branding": Verify brand colors referenced in prompts
3. Each test provides mock customer data and modules, checks output string contains expected patterns.
  </action>
  <verify>
    <automated>cd C:/Users/scott/Documents/guardian-intel && npx vitest run src/features/infographic-generator/services/__tests__/modelIntelligence.test.ts src/features/infographic-generator/utils/__tests__/promptComposer.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/features/infographic-generator/services/__tests__/modelIntelligence.test.ts
    - test -f src/features/infographic-generator/utils/__tests__/promptComposer.test.ts
    - grep -q "scoreRequest\|scoring" src/features/infographic-generator/services/__tests__/modelIntelligence.test.ts
    - grep -q "composePrompt\|prompt" src/features/infographic-generator/utils/__tests__/promptComposer.test.ts
  </acceptance_criteria>
  <done>Unit tests for model intelligence scoring and prompt composer template generation.</done>
</task>

<task type="auto">
  <name>Task 2: Intent parser unit tests and E2E generation tests</name>
  <files>
    src/features/infographic-generator/utils/__tests__/intentParser.test.ts
    tests/e2e/infographic-generation.spec.ts
  </files>
  <read_first>
    src/features/infographic-generator/utils/intentParser.ts
    src/features/infographic-generator/utils/__tests__/infographicDataAssembler.test.ts
    playwright.config.ts
  </read_first>
  <action>
Create src/features/infographic-generator/utils/__tests__/intentParser.test.ts:

1. Import parseIntent or relevant functions from intentParser.
2. Mock AI router/fetch calls (vi.fn() on global fetch or mock the AI service).
3. Test suites:
   - "intent classification": Test natural language → module mapping
     - "prep me for the Johnson meeting" → Meeting Prep modules
     - "storm damage for this area" → Post-Storm modules
     - "create a leave-behind" → Customer Leave-Behind, customer-facing audience
     - "competitive analysis" → Competitive Edge modules
   - "audience detection": Test audience inference
     - Meeting-related prompts → internal audience
     - "leave-behind" / "send to customer" → customer-facing
   - "confidence scoring": Test confidence levels
     - Clear, specific prompts → high confidence
     - Vague prompts → lower confidence
4. Mock the AI API call to return structured responses.

Create tests/e2e/infographic-generation.spec.ts:

1. Import test, expect from @playwright/test.
2. Test suites (these test the UI flow, may need API mocking):
   - "single generation flow":
     - Navigate to dashboard
     - Open a customer card
     - Click BRIEFING button
     - Verify modal opens with three tabs
     - Select a preset
     - Click Generate Briefing
     - Verify progress display appears
     - (Mock API response for speed)
   - "custom topic generation":
     - Open modal, switch to Custom tab
     - Toggle audience to Customer-Facing
     - Select multiple modules
     - Click Generate
   - "conversational generation":
     - Open modal, switch to Ask AI tab
     - Type a prompt or click a chip
     - Click Generate
   - "batch generation":
     - Navigate to dashboard
     - Click Prep My Day
     - Verify BatchDayView appears
     - Verify progressive results display
   - "share flow":
     - After generation, verify share options available
     - Test copy link functionality
3. Use page.route() to mock API responses for deterministic testing.
4. Follow patterns from existing E2E tests if any exist, otherwise follow Playwright best practices.
  </action>
  <verify>
    <automated>cd C:/Users/scott/Documents/guardian-intel && npx vitest run src/features/infographic-generator/utils/__tests__/intentParser.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/features/infographic-generator/utils/__tests__/intentParser.test.ts
    - test -f tests/e2e/infographic-generation.spec.ts
    - grep -q "parseIntent\|intent" src/features/infographic-generator/utils/__tests__/intentParser.test.ts
    - grep -q "BRIEFING\|Generate Briefing\|infographic" tests/e2e/infographic-generation.spec.ts
    - grep -q "Prep My Day\|batch" tests/e2e/infographic-generation.spec.ts
  </acceptance_criteria>
  <done>Intent parser unit tests cover classification and confidence. E2E tests cover single/custom/conversational/batch generation and sharing flows.</done>
</task>

</tasks>

<verification>
- Unit tests pass via `npx vitest run`
- All 4 test files exist
- Tests cover model scoring, prompt templates, intent classification
- E2E tests cover generation and sharing flows
</verification>

<success_criteria>
- Model intelligence tests verify scoring dimensions and model/chain selection
- Prompt composer tests verify model-specific template generation
- Intent parser tests verify natural language classification
- E2E tests cover all generation modes and batch flow
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-polish/06-03-SUMMARY.md`
</output>
