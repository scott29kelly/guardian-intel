---
phase: 01-foundation-model-intelligence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/ai/adapters/gemini-flash-image.ts
  - src/lib/services/ai/types.ts
  - src/lib/services/ai/router.ts
autonomous: true
requirements: [INFOG-004, INFOG-005]

must_haves:
  truths:
    - "Gemini Flash Image adapter accepts a prompt and returns a generated image with retry on failure (exponential backoff 1s, 2s, 4s)"
    - "AI Router routes an image_generation task through the adapter without exposing model choice to callers"
    - "AI types include gemini-3.1-flash-image-preview model, image_generation task, and ImageGenerationRequest/Response interfaces"
  artifacts:
    - path: "src/lib/services/ai/adapters/gemini-flash-image.ts"
      provides: "NB2 image generation adapter with web search grounding and retry"
      exports: ["GeminiFlashImageAdapter", "createGeminiFlashImageAdapter"]
    - path: "src/lib/services/ai/types.ts"
      provides: "Extended AI types with image generation support"
      contains: "image_generation"
    - path: "src/lib/services/ai/router.ts"
      provides: "Router with generateImage method and image_generation in TASK_MODEL_MAP"
      exports: ["AIRouter", "getAIRouter"]
  key_links:
    - from: "src/lib/services/ai/adapters/gemini-flash-image.ts"
      to: "src/lib/services/ai/types.ts"
      via: "imports AIAdapter, ImageGenerationRequest, ImageGenerationResponse"
      pattern: "import.*from.*types"
    - from: "src/lib/services/ai/router.ts"
      to: "src/lib/services/ai/types.ts"
      via: "imports ImageGenerationRequest, ImageGenerationResponse"
      pattern: "ImageGenerationRequest"
    - from: "src/lib/services/ai/router.ts"
      to: "src/lib/services/ai/adapters/gemini-flash-image.ts"
      via: "adapter registration and generateImage delegation"
      pattern: "generateImage"
---

<objective>
Verify and commit the AI infrastructure: Gemini Flash Image adapter (INFOG-004) and AI Router updates (INFOG-005).

Purpose: The adapter is the execution layer that calls the Gemini API for image generation, and the router updates wire it into the existing AI service architecture. These must be correct for the generation engine (Phase 4) to function.

Output: Three verified and committed files extending the AI service layer with image generation capability.
</objective>

<execution_context>
@C:/Users/scott/Documents/guardian-intel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/scott/Documents/guardian-intel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@INFOGRAPHIC_PRD.md
</context>

<interfaces>
<!-- AI adapter interface that gemini-flash-image must implement -->

From src/lib/services/ai/types.ts:
```typescript
export interface AIAdapter {
  readonly provider: AIProvider;
  readonly model: AIModel | string;
  chat(request: ChatRequest): Promise<ChatResponse>;
  chatStream?(request: ChatRequest): AsyncIterable<StreamChunk>;
  research?(request: ResearchRequest): Promise<ResearchResult>;
  classify?(request: ClassifyRequest): Promise<ClassifyResult>;
  parse?(request: ParseRequest): Promise<ParseResult>;
}

export interface ImageGenerationRequest {
  prompt: string;
  referenceImages?: Array<{ data: string; mimeType?: string; }>;
  searchTypes?: ("web" | "image")[];
  resolution?: "1K" | "2K" | "4K";
}

export interface ImageGenerationResponse {
  imageData: string; // base64
  model: string;
}
```

From src/lib/services/ai/router.ts:
```typescript
export class AIRouter {
  registerAdapter(adapter: AIAdapter): void;
  async generateImage(request: ImageGenerationRequest, model?: AIModel): Promise<ImageGenerationResponse>;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify Gemini Flash Image adapter against PRD spec (INFOG-004)</name>
  <read_first>
    - INFOGRAPHIC_PRD.md (Group 1 spec: INFOG-004 adapter requirements)
    - src/lib/services/ai/adapters/gemini-flash-image.ts
    - src/lib/services/ai/types.ts (AIAdapter interface it must implement)
    - src/features/deck-generator/services/slideImageGenerator.ts (retry pattern reference -- read first 50 lines for pattern)
  </read_first>
  <files>
    src/lib/services/ai/adapters/gemini-flash-image.ts
  </files>
  <action>
    Review gemini-flash-image.ts against PRD INFOG-004 spec. Verify ALL of these:

    1. Implements AIAdapter interface (has chat() method with correct signature)
    2. Has generateImage() method accepting ImageGenerationRequest
    3. Supports search_types parameter for web + image search grounding (passes googleSearch tool to API)
    4. Accepts reference images (up to 14 for NB2) -- slices to 14 max
    5. Retry logic with exponential backoff: baseDelayMs 1000 (1s, 2s, 4s pattern via Math.pow(2, attempt))
    6. maxRetries defaults to 3
    7. Non-retryable errors (401, 403) do NOT retry
    8. Returns base64 image data in ImageGenerationResponse format
    9. Provider is 'google', model is 'gemini-3.1-flash-image-preview'
    10. Resolution tiers (1K, 2K, 4K) -- check if resolution parameter is used in API call. NOTE: Gemini API may not support explicit resolution parameter; if the adapter accepts it in the request type but doesn't pass it to the API, that is acceptable since the API may handle resolution internally.

    If any requirement is missing or incorrect, fix it. If everything matches, no changes needed.
  </action>
  <verify>
    <automated>cd C:/Users/scott/Documents/guardian-intel && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "implements AIAdapter" src/lib/services/ai/adapters/gemini-flash-image.ts returns >= 1
    - grep -c "generateImage" src/lib/services/ai/adapters/gemini-flash-image.ts returns >= 2
    - grep -c "googleSearch" src/lib/services/ai/adapters/gemini-flash-image.ts returns >= 1
    - grep -c "slice(0, 14)" src/lib/services/ai/adapters/gemini-flash-image.ts returns >= 1
    - grep -c "baseDelayMs.*1000" src/lib/services/ai/adapters/gemini-flash-image.ts returns >= 1
    - grep -c "Math.pow(2" src/lib/services/ai/adapters/gemini-flash-image.ts returns >= 1
    - grep -c "maxRetries.*3" src/lib/services/ai/adapters/gemini-flash-image.ts returns >= 1
    - grep -c "nonRetryable" src/lib/services/ai/adapters/gemini-flash-image.ts returns >= 1
    - grep -c "imageData" src/lib/services/ai/adapters/gemini-flash-image.ts returns >= 1
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Gemini Flash Image adapter implements AIAdapter, has generateImage with web search grounding, reference image support (max 14), exponential backoff retry (1s/2s/4s), and returns base64 image data.</done>
</task>

<task type="auto">
  <name>Task 2: Verify AI Router and types updates against PRD spec (INFOG-005)</name>
  <read_first>
    - INFOGRAPHIC_PRD.md (Group 1 spec: INFOG-005 router updates)
    - src/lib/services/ai/types.ts
    - src/lib/services/ai/router.ts
  </read_first>
  <files>
    src/lib/services/ai/types.ts
    src/lib/services/ai/router.ts
  </files>
  <action>
    Review types.ts against PRD INFOG-005 spec. Verify:
    1. AIModel union includes "gemini-3.1-flash-image-preview" (NB2)
    2. AIModel union includes "gemini-3-pro-image" (NB Pro)
    3. AITask union includes "image_generation"
    4. ImageGenerationRequest interface exists with: prompt, referenceImages?, searchTypes?, resolution?
    5. ImageGenerationResponse interface exists with: imageData (string), model (string)

    Review router.ts against PRD INFOG-005 spec. Verify:
    1. TASK_MODEL_MAP includes image_generation mapped to "gemini-3.1-flash-image-preview"
    2. generateImage(request, model?) method exists on AIRouter class
    3. generateImage delegates to adapter's generateImage method if available
    4. generateImage falls back to chat if adapter lacks generateImage
    5. generateImage accepts optional model parameter (for Model Intelligence to override default)
    6. Imports ImageGenerationRequest and ImageGenerationResponse from types

    If any requirement is missing, add it. If everything matches, no changes needed.
  </action>
  <verify>
    <automated>cd C:/Users/scott/Documents/guardian-intel && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "gemini-3.1-flash-image-preview" src/lib/services/ai/types.ts returns >= 1
    - grep -c "gemini-3-pro-image" src/lib/services/ai/types.ts returns >= 1
    - grep -c "image_generation" src/lib/services/ai/types.ts returns >= 1
    - grep -c "ImageGenerationRequest" src/lib/services/ai/types.ts returns >= 1
    - grep -c "ImageGenerationResponse" src/lib/services/ai/types.ts returns >= 1
    - grep -c "image_generation" src/lib/services/ai/router.ts returns >= 1
    - grep -c "generateImage" src/lib/services/ai/router.ts returns >= 2
    - grep -c "ImageGenerationRequest" src/lib/services/ai/router.ts returns >= 1
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>AI types include NB2 and NB Pro models, image_generation task, and request/response interfaces. Router has image_generation in TASK_MODEL_MAP and generateImage method with model override support.</done>
</task>

<task type="auto">
  <name>Task 3: Commit verified adapter and router files (INFOG-004, INFOG-005)</name>
  <read_first>
    - src/lib/services/ai/adapters/gemini-flash-image.ts
    - src/lib/services/ai/types.ts
    - src/lib/services/ai/router.ts
  </read_first>
  <files>
    src/lib/services/ai/adapters/gemini-flash-image.ts
    src/lib/services/ai/types.ts
    src/lib/services/ai/router.ts
  </files>
  <action>
    After Tasks 1 and 2 pass verification, stage and commit the three AI infrastructure files:
    - src/lib/services/ai/adapters/gemini-flash-image.ts (new file)
    - src/lib/services/ai/types.ts (modified -- added image gen types)
    - src/lib/services/ai/router.ts (modified -- added generateImage method)

    Commit message: "feat(ai): add Gemini Flash Image adapter and router image generation support (INFOG-004, INFOG-005)"

    Do NOT stage or commit files from plan 01 (infographic-generator/types, utils, services). Those are handled separately.
  </action>
  <verify>
    <automated>cd C:/Users/scott/Documents/guardian-intel && git log --oneline -1 | grep -c "INFOG-004"</automated>
  </verify>
  <acceptance_criteria>
    - git log --oneline -1 contains "INFOG-004"
    - git diff --cached is empty (everything committed)
    - git status shows no modifications to src/lib/services/ai/types.ts
    - git status shows no modifications to src/lib/services/ai/router.ts
    - git status shows no untracked src/lib/services/ai/adapters/gemini-flash-image.ts
  </acceptance_criteria>
  <done>Adapter and router files committed to git with descriptive message referencing requirement IDs.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes with zero errors
- All three files are tracked in git (committed)
- Adapter implements AIAdapter interface with generateImage, retry, and web search
- Router has image_generation task routing and generateImage method
- Types include both NB2 and NB Pro model identifiers
</verification>

<success_criteria>
- gemini-flash-image.ts implements AIAdapter, has generateImage with retry (1s/2s/4s), web search grounding, reference images (max 14)
- types.ts has NB2 + NB Pro in AIModel, image_generation in AITask, ImageGenerationRequest/Response interfaces
- router.ts has image_generation in TASK_MODEL_MAP, generateImage method with model override
- All three files committed to git
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-model-intelligence/01-02-SUMMARY.md`
</output>
