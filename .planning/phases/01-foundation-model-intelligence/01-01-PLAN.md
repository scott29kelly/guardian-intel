---
phase: 01-foundation-model-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/infographic-generator/types/infographic.types.ts
  - src/features/infographic-generator/utils/brandingAssets.ts
  - src/features/infographic-generator/services/modelIntelligence.ts
autonomous: true
requirements: [INFOG-001, INFOG-002, INFOG-003]

must_haves:
  truths:
    - "All infographic type definitions compile and cover every generation entity (modes, presets, modules, models, scoring, requests, responses, progress, cache, batch)"
    - "Branding assets provide dark theme (Navy/Gold/Teal) for internal and light theme for customer-facing, with audience-based helper"
    - "Model Intelligence scores a request across 3 dimensions (audience, complexity, web search) and selects NB2, NB Pro, or a chain strategy"
  artifacts:
    - path: "src/features/infographic-generator/types/infographic.types.ts"
      provides: "All TypeScript types for infographic generation"
      contains: "GenerationMode"
    - path: "src/features/infographic-generator/utils/brandingAssets.ts"
      provides: "Branding configs and audience helper"
      exports: ["infographicBrandConfig", "infographicLightBrandConfig", "getBrandingForInfographic"]
    - path: "src/features/infographic-generator/services/modelIntelligence.ts"
      provides: "Model registry, scoring engine, chain strategies, singleton"
      exports: ["ModelRegistry", "scoreRequest", "getModelIntelligence"]
  key_links:
    - from: "src/features/infographic-generator/services/modelIntelligence.ts"
      to: "src/features/infographic-generator/types/infographic.types.ts"
      via: "imports ModelCapabilities, ScoringResult, InfographicRequest, TopicModule"
      pattern: "import.*from.*types/infographic.types"
    - from: "src/features/infographic-generator/utils/brandingAssets.ts"
      to: "src/features/deck-generator/types/deck.types.ts"
      via: "imports BrandingConfig type"
      pattern: "import.*BrandingConfig.*from.*deck-generator"
---

<objective>
Verify and commit the foundation layer: TypeScript types (INFOG-001), branding assets (INFOG-002), and Model Intelligence scoring engine (INFOG-003).

Purpose: These three files are the type contracts and core intelligence that every downstream phase depends on. Verifying them first ensures the baseline is correct before building data layer, templates, and generation engine on top.

Output: Three verified and committed files forming the infographic generator's type system, branding, and autonomous model selection.
</objective>

<execution_context>
@C:/Users/scott/Documents/guardian-intel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/scott/Documents/guardian-intel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@INFOGRAPHIC_PRD.md
</context>

<interfaces>
<!-- Key types from deck generator that infographic types/branding extend -->

From src/features/deck-generator/types/deck.types.ts:
```typescript
export interface BrandingConfig {
  colors: {
    primary: string; secondary: string; accent: string;
    background: string; backgroundAlt: string; text: string; textMuted: string;
    success: string; warning: string; danger: string;
  };
  fonts: { heading: string; body: string; mono: string; };
  logo: string; logoAlt?: string; footer: string; borderRadius: string;
}
```

From src/lib/services/ai/types.ts:
```typescript
export type AIModel =
  | "gemini-2.0-flash-exp" | "gemini-1.5-pro"
  | "gemini-3.1-flash-image-preview" | "gemini-3-pro-image"
  | "kimi-k2" | "claude-opus-4.5" | "claude-sonnet-4.5" | "claude-haiku-4.5"
  | "perplexity-sonar" | "gpt-4o";
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify types and branding against PRD spec (INFOG-001, INFOG-002)</name>
  <read_first>
    - INFOGRAPHIC_PRD.md (Groups 0 spec: INFOG-001 types list, INFOG-002 branding spec)
    - src/features/infographic-generator/types/infographic.types.ts
    - src/features/infographic-generator/utils/brandingAssets.ts
    - src/features/deck-generator/types/deck.types.ts (pattern reference)
    - src/features/deck-generator/utils/brandingConfig.ts (pattern reference)
  </read_first>
  <files>
    src/features/infographic-generator/types/infographic.types.ts
    src/features/infographic-generator/utils/brandingAssets.ts
  </files>
  <action>
    Review the existing infographic.types.ts against PRD INFOG-001 spec. Verify ALL of these types exist:
    - GenerationMode = 'preset' | 'custom' | 'conversational'
    - InfographicPreset with id, name, description, audience, modules (TopicModuleConfig[]), usageMoment, icon
    - TopicModule with id, label, dataSource, requiresWebSearch, visualElement
    - TopicModuleConfig with module, enabled, required
    - ModelCapabilities with id (AIModel), costPerImage1K, maxResolution, webSearchGrounding, maxReferenceImages, fidelityScore, textRenderingScore, complexCompositionScore, chainable
    - ModelChainStrategy with id, name, trigger, steps (ModelChainStep[])
    - ScoringResult with selectedModel or selectedChain, scores, reasoning
    - InfographicRequest with customerId, mode, presetId?, selectedModules?, conversationalPrompt?, audience?
    - InfographicResponse with imageData, imageUrl?, model, chainUsed, generationTimeMs, cached
    - InfographicCacheEntry with customerId, presetId, imageData, generatedAt, expiresAt, modelStrategy
    - BatchRequest with customerIds[], autoSelectPresets
    - GenerationProgress with phase, percent, statusMessage
    - NO QualityTier type (removed in v3 per PRD)

    Review brandingAssets.ts against PRD INFOG-002 spec. Verify:
    - infographicBrandConfig has dark theme: primary #1E3A5F, secondary #D4A656, accent #4A90A4, background #0F1419
    - infographicLightBrandConfig has light theme: white background, navy text
    - getBrandingForInfographic(audience) returns correct config per audience
    - Brand reference images exported (logo, watermark)
    - Uses BrandingConfig type from deck-generator (reuse, not redefine)

    If any type is MISSING or has wrong shape, add or fix it. If everything matches, no changes needed.
  </action>
  <verify>
    <automated>cd C:/Users/scott/Documents/guardian-intel && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "GenerationMode" src/features/infographic-generator/types/infographic.types.ts returns >= 1
    - grep -c "InfographicPreset" src/features/infographic-generator/types/infographic.types.ts returns >= 1
    - grep -c "TopicModule" src/features/infographic-generator/types/infographic.types.ts returns >= 1
    - grep -c "ModelCapabilities" src/features/infographic-generator/types/infographic.types.ts returns >= 1
    - grep -c "ScoringResult" src/features/infographic-generator/types/infographic.types.ts returns >= 1
    - grep -c "InfographicRequest" src/features/infographic-generator/types/infographic.types.ts returns >= 1
    - grep -c "InfographicResponse" src/features/infographic-generator/types/infographic.types.ts returns >= 1
    - grep -c "GenerationProgress" src/features/infographic-generator/types/infographic.types.ts returns >= 1
    - grep -c "BatchRequest" src/features/infographic-generator/types/infographic.types.ts returns >= 1
    - grep -c "InfographicCacheEntry" src/features/infographic-generator/types/infographic.types.ts returns >= 1
    - grep "QualityTier" src/features/infographic-generator/types/infographic.types.ts returns 0 matches (removed in v3)
    - grep -c "#1E3A5F" src/features/infographic-generator/utils/brandingAssets.ts returns >= 1
    - grep -c "#D4A656" src/features/infographic-generator/utils/brandingAssets.ts returns >= 1
    - grep -c "getBrandingForInfographic" src/features/infographic-generator/utils/brandingAssets.ts returns >= 1
    - grep -c "infographicLightBrandConfig" src/features/infographic-generator/utils/brandingAssets.ts returns >= 1
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>All PRD-specified types exist in infographic.types.ts, branding assets match color spec, audience helper works, and TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Model Intelligence scoring engine against PRD spec (INFOG-003)</name>
  <read_first>
    - INFOGRAPHIC_PRD.md (Group 1 spec: INFOG-003 model registry + scoring)
    - src/features/infographic-generator/services/modelIntelligence.ts
    - src/features/infographic-generator/types/infographic.types.ts
  </read_first>
  <files>
    src/features/infographic-generator/services/modelIntelligence.ts
  </files>
  <action>
    Review modelIntelligence.ts against PRD INFOG-003 spec. Verify ALL of these:

    1. ModelRegistry class with register() and getAll() methods
    2. NB2 pre-registered with: id gemini-3.1-flash-image-preview, costPerImage1K 0.067, webSearchGrounding true, maxReferenceImages 14, fidelityScore 7, complexCompositionScore 6, chainable 'source'
    3. NB Pro pre-registered with: id gemini-3-pro-image, costPerImage1K 0.27, webSearchGrounding false, maxReferenceImages 10, fidelityScore 9, complexCompositionScore 9, chainable 'finisher'
    4. scoreRequest(request, modules) evaluates 3 dimensions:
       - Audience: internal (moderate) vs customer-facing (max fidelity, 2K floor)
       - Complexity: Low 1-3 (NB2), Medium 4-6 (NB Pro preferred), High 7+ (NB Pro strongly)
       - Web Search: HARD CONSTRAINT -- requiresWebSearch true on any module forces web-capable model
    5. Chain A (Web-Grounded Quality): web search + customer-facing OR high complexity -> NB2@1K -> NB Pro@2K-4K
    6. Chain B (Complexity Upgrade): 7+ modules + internal + no web -> NB2@1K -> NB Pro@2K
    7. Chain C (Batch Elevation): per-customer scoring -- verify this is described or handled
    8. Singleton getModelIntelligence() accessor

    If Chain C is missing: it is per-customer independent scoring in batch mode, which is a runtime concern handled by the batch hook (Phase 5). If modelIntelligence.ts does not define it as a chain strategy, that is ACCEPTABLE -- note it in the summary but do not add it here.

    If any scoring logic is incorrect or missing, fix it. If everything matches, no changes needed.
  </action>
  <verify>
    <automated>cd C:/Users/scott/Documents/guardian-intel && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ModelRegistry" src/features/infographic-generator/services/modelIntelligence.ts returns >= 1
    - grep -c "scoreRequest" src/features/infographic-generator/services/modelIntelligence.ts returns >= 1
    - grep -c "getModelIntelligence" src/features/infographic-generator/services/modelIntelligence.ts returns >= 1
    - grep -c "gemini-3.1-flash-image-preview" src/features/infographic-generator/services/modelIntelligence.ts returns >= 1
    - grep -c "gemini-3-pro-image" src/features/infographic-generator/services/modelIntelligence.ts returns >= 1
    - grep -c "chain-a" src/features/infographic-generator/services/modelIntelligence.ts returns >= 1
    - grep -c "chain-b" src/features/infographic-generator/services/modelIntelligence.ts returns >= 1
    - grep -c "webSearchGrounding" src/features/infographic-generator/services/modelIntelligence.ts returns >= 1
    - grep -c "requiresWebSearch" src/features/infographic-generator/services/modelIntelligence.ts returns >= 1
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Model Intelligence has registry with NB2+NB Pro profiles, 3-dimension scoring, Chain A and B strategies, and singleton accessor. All compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Commit verified foundation files (INFOG-001, INFOG-002, INFOG-003)</name>
  <read_first>
    - src/features/infographic-generator/types/infographic.types.ts
    - src/features/infographic-generator/utils/brandingAssets.ts
    - src/features/infographic-generator/services/modelIntelligence.ts
  </read_first>
  <files>
    src/features/infographic-generator/types/infographic.types.ts
    src/features/infographic-generator/utils/brandingAssets.ts
    src/features/infographic-generator/services/modelIntelligence.ts
  </files>
  <action>
    After Tasks 1 and 2 pass verification, stage and commit the three foundation files:
    - src/features/infographic-generator/types/infographic.types.ts
    - src/features/infographic-generator/utils/brandingAssets.ts
    - src/features/infographic-generator/services/modelIntelligence.ts

    Commit message: "feat(infographic): add foundation types, branding assets, and model intelligence (INFOG-001, INFOG-002, INFOG-003)"

    Do NOT stage or commit any other files in this task. The adapter and router changes belong to plan 02.
  </action>
  <verify>
    <automated>cd C:/Users/scott/Documents/guardian-intel && git log --oneline -1 | grep -c "INFOG-001"</automated>
  </verify>
  <acceptance_criteria>
    - git log --oneline -1 contains "INFOG-001"
    - git status shows src/features/infographic-generator/types/infographic.types.ts is committed (not in working tree changes)
    - git status shows src/features/infographic-generator/utils/brandingAssets.ts is committed
    - git status shows src/features/infographic-generator/services/modelIntelligence.ts is committed
  </acceptance_criteria>
  <done>Foundation files committed to git with descriptive message referencing requirement IDs.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes with zero errors
- All three files are tracked in git (committed)
- Types cover every entity from PRD INFOG-001
- Branding colors match PRD spec (#1E3A5F, #D4A656, #4A90A4)
- Model Intelligence scoring handles all 6 decision cases from PRD
</verification>

<success_criteria>
- infographic.types.ts exports all 12+ type definitions from PRD without QualityTier
- brandingAssets.ts provides dark/light themes with correct hex codes and audience helper
- modelIntelligence.ts has registry, 3-dimension scoring, Chain A/B, and singleton
- All three files committed to git
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-model-intelligence/01-01-SUMMARY.md`
</output>
