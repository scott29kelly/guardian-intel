---
phase: 03-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/infographic-generator/templates/pre-knock-briefing.ts
  - src/features/infographic-generator/templates/post-storm-followup.ts
  - src/features/infographic-generator/templates/insurance-meeting-prep.ts
  - src/features/infographic-generator/templates/competitive-edge.ts
  - src/features/infographic-generator/templates/customer-leave-behind.ts
autonomous: true
requirements: [INFOG-009, INFOG-010, INFOG-011]

must_haves:
  truths:
    - "Pre-Knock Briefing preset defines 6 modules targeting internal audience with 'Parking Lot' usage moment"
    - "Post-Storm Follow-up preset defines 5 modules including web search modules, targeting internal audience with 'Post-Storm' usage moment"
    - "Insurance Meeting Prep preset defines 5 modules with web search flags, targeting internal audience with 'Meeting Prep' usage moment"
    - "Competitive Edge preset defines 5 modules targeting internal audience with 'Meeting Prep' usage moment"
    - "Customer Leave-Behind preset defines 6 modules targeting customer-facing audience with 'Leave-Behind' usage moment"
  artifacts:
    - path: "src/features/infographic-generator/templates/pre-knock-briefing.ts"
      provides: "Pre-Knock Briefing preset definition"
      exports: ["preKnockBriefingPreset"]
      contains: "InfographicPreset"
    - path: "src/features/infographic-generator/templates/post-storm-followup.ts"
      provides: "Post-Storm Follow-up preset definition"
      exports: ["postStormFollowupPreset"]
      contains: "InfographicPreset"
    - path: "src/features/infographic-generator/templates/insurance-meeting-prep.ts"
      provides: "Insurance Meeting Prep preset definition"
      exports: ["insuranceMeetingPrepPreset"]
      contains: "InfographicPreset"
    - path: "src/features/infographic-generator/templates/competitive-edge.ts"
      provides: "Competitive Edge preset definition"
      exports: ["competitiveEdgePreset"]
      contains: "InfographicPreset"
    - path: "src/features/infographic-generator/templates/customer-leave-behind.ts"
      provides: "Customer Leave-Behind preset definition"
      exports: ["customerLeaveBehindPreset"]
      contains: "InfographicPreset"
  key_links:
    - from: "all template files"
      to: "src/features/infographic-generator/services/intentParser.ts"
      via: "AVAILABLE_MODULES reference"
      pattern: "AVAILABLE_MODULES\\.find"
    - from: "all template files"
      to: "src/features/infographic-generator/types/infographic.types.ts"
      via: "InfographicPreset type import"
      pattern: "import.*InfographicPreset"
---

<objective>
Create all five infographic preset template files -- the data definitions that describe what modules each preset contains, which audience it targets, and what usage moment it belongs to.

Purpose: These presets are the content configurations that drive infographic generation. Each one composes TopicModules from AVAILABLE_MODULES into a purpose-specific briefing for reps. Templates NEVER specify models -- Model Intelligence handles that autonomously.

Output: 5 TypeScript preset definition files in src/features/infographic-generator/templates/
</objective>

<execution_context>
@C:/Users/scott/Documents/guardian-intel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/scott/Documents/guardian-intel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/features/infographic-generator/types/infographic.types.ts
@src/features/infographic-generator/services/intentParser.ts
@src/features/infographic-generator/utils/brandingAssets.ts
@src/features/deck-generator/templates/customer-cheat-sheet.ts

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From src/features/infographic-generator/types/infographic.types.ts:
```typescript
export type InfographicAudience = 'internal' | 'customer-facing';
export type UsageMoment = 'Parking Lot' | 'Post-Storm' | 'Meeting Prep' | 'Leave-Behind' | 'Batch';

export interface TopicModule {
  id: string;
  label: string;
  dataSource: string;
  requiresWebSearch: boolean;
  visualElement: string;
}

export interface TopicModuleConfig {
  module: TopicModule;
  enabled: boolean;
  required: boolean;
}

export interface InfographicPreset {
  id: string;
  name: string;
  description: string;
  audience: InfographicAudience;
  modules: TopicModuleConfig[];
  usageMoment: UsageMoment;
  icon: string; // Lucide icon name
}
```

From src/features/infographic-generator/services/intentParser.ts:
```typescript
export const AVAILABLE_MODULES: TopicModule[] = [
  { id: "property-overview", label: "Property Overview", dataSource: "getPropertyIntelData", requiresWebSearch: false, ... },
  { id: "roof-assessment", label: "Roof Assessment", dataSource: "getPropertyIntelData", requiresWebSearch: false, ... },
  { id: "storm-timeline", label: "Storm History", dataSource: "getStormHistoryTimeline", requiresWebSearch: false, ... },
  { id: "lead-scoring", label: "Lead Scoring", dataSource: "getCustomerOverviewStats", requiresWebSearch: false, ... },
  { id: "interaction-history", label: "Interaction History", dataSource: "daysSinceLastContact", requiresWebSearch: false, ... },
  { id: "next-steps", label: "Next Steps", dataSource: "getCustomerOverviewStats", requiresWebSearch: false, ... },
  { id: "live-weather", label: "Live Weather", dataSource: "getStormHistoryTimeline", requiresWebSearch: true, ... },
  { id: "neighborhood-context", label: "Neighborhood Context", dataSource: "neighborhoodConversionRate", requiresWebSearch: false, ... },
  { id: "insurance-status", label: "Insurance Status", dataSource: "insuranceDeadlineCountdown", requiresWebSearch: false, ... },
  { id: "carrier-intel", label: "Carrier Intel", dataSource: "getCustomerOverviewStats", requiresWebSearch: true, ... },
  { id: "competitor-landscape", label: "Competitor Landscape", dataSource: "getCustomerOverviewStats", requiresWebSearch: false, ... },
  { id: "company-credentials", label: "Company Credentials", dataSource: "getCustomerTitleData", requiresWebSearch: false, ... },
  { id: "contact-info", label: "Contact Info", dataSource: "getCustomerTitleData", requiresWebSearch: false, ... },
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pre-Knock Briefing and Post-Storm Follow-up presets (INFOG-009)</name>
  <files>
    src/features/infographic-generator/templates/pre-knock-briefing.ts
    src/features/infographic-generator/templates/post-storm-followup.ts
  </files>
  <read_first>
    src/features/infographic-generator/types/infographic.types.ts
    src/features/infographic-generator/services/intentParser.ts
    src/features/deck-generator/templates/customer-cheat-sheet.ts
  </read_first>
  <action>
Create the templates directory at src/features/infographic-generator/templates/ if it does not exist.

**pre-knock-briefing.ts** -- Per INFOG-009 and the PRD:
- Import `InfographicPreset`, `TopicModuleConfig` from `../types/infographic.types`
- Import `AVAILABLE_MODULES` from `../services/intentParser`
- Create a helper function `findModule(id: string)` that does `AVAILABLE_MODULES.find(m => m.id === id)!` to look up TopicModule by ID
- Export `const preKnockBriefingPreset: InfographicPreset` with:
  - `id: 'pre-knock-briefing'`
  - `name: 'Pre-Knock Briefing'`
  - `description: 'Quick 60-second prep before knocking on a door. Property intel, storm history, and talking points at a glance.'`
  - `audience: 'internal'`
  - `usageMoment: 'Parking Lot'`
  - `icon: 'DoorOpen'` (Lucide icon)
  - `modules`: 6 TopicModuleConfig entries using findModule():
    1. `property-overview` -- enabled: true, required: true
    2. `roof-assessment` -- enabled: true, required: false
    3. `storm-timeline` -- enabled: true, required: false
    4. `lead-scoring` -- enabled: true, required: false
    5. `interaction-history` -- enabled: true, required: false
    6. `next-steps` -- enabled: true, required: false

**post-storm-followup.ts** -- Per INFOG-009 and the PRD:
- Same imports pattern as above
- Export `const postStormFollowupPreset: InfographicPreset` with:
  - `id: 'post-storm-followup'`
  - `name: 'Post-Storm Follow-up'`
  - `description: 'Storm response briefing with live weather, damage timeline, and insurance filing deadlines.'`
  - `audience: 'internal'`
  - `usageMoment: 'Post-Storm'`
  - `icon: 'CloudLightning'` (Lucide icon)
  - `modules`: 5 TopicModuleConfig entries:
    1. `storm-timeline` -- enabled: true, required: true
    2. `live-weather` -- enabled: true, required: false (note: requiresWebSearch=true on this module)
    3. `neighborhood-context` -- enabled: true, required: false
    4. `insurance-status` -- enabled: true, required: false
    5. `filing-deadline` -- This module does not exist in AVAILABLE_MODULES. Use `insurance-status` for the filing deadline data (its dataSource is `insuranceDeadlineCountdown`). So the 5th module is `next-steps` -- enabled: true, required: false

IMPORTANT: Each template file should NOT duplicate TopicModule definitions. Always reference AVAILABLE_MODULES from intentParser.ts via the findModule helper. Templates define which modules to include and whether they are enabled/required -- nothing more.

Do NOT specify any model, quality tier, or chain strategy in templates. Model Intelligence handles that.
  </action>
  <verify>
    <automated>cd "C:/Users/scott/Documents/guardian-intel" && npx tsc --noEmit src/features/infographic-generator/templates/pre-knock-briefing.ts src/features/infographic-generator/templates/post-storm-followup.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "preKnockBriefingPreset" src/features/infographic-generator/templates/pre-knock-briefing.ts
    - grep -q "audience.*internal" src/features/infographic-generator/templates/pre-knock-briefing.ts
    - grep -q "Parking Lot" src/features/infographic-generator/templates/pre-knock-briefing.ts
    - grep -q "postStormFollowupPreset" src/features/infographic-generator/templates/post-storm-followup.ts
    - grep -q "audience.*internal" src/features/infographic-generator/templates/post-storm-followup.ts
    - grep -q "Post-Storm" src/features/infographic-generator/templates/post-storm-followup.ts
    - grep -q "AVAILABLE_MODULES" src/features/infographic-generator/templates/pre-knock-briefing.ts
    - grep -q "AVAILABLE_MODULES" src/features/infographic-generator/templates/post-storm-followup.ts
  </acceptance_criteria>
  <done>Pre-Knock Briefing exports preset with 6 modules, internal audience, "Parking Lot" moment. Post-Storm Follow-up exports preset with 5 modules including web-search-requiring live-weather, internal audience, "Post-Storm" moment. Both reference AVAILABLE_MODULES, not inline definitions. Both compile with zero type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create Insurance Meeting Prep, Competitive Edge, and Customer Leave-Behind presets (INFOG-010, INFOG-011)</name>
  <files>
    src/features/infographic-generator/templates/insurance-meeting-prep.ts
    src/features/infographic-generator/templates/competitive-edge.ts
    src/features/infographic-generator/templates/customer-leave-behind.ts
  </files>
  <read_first>
    src/features/infographic-generator/types/infographic.types.ts
    src/features/infographic-generator/services/intentParser.ts
    src/features/infographic-generator/templates/pre-knock-briefing.ts
  </read_first>
  <action>
Follow the exact same pattern established in Task 1 (import AVAILABLE_MODULES, use findModule helper, export typed InfographicPreset).

**insurance-meeting-prep.ts** -- Per INFOG-010:
- Export `const insuranceMeetingPrepPreset: InfographicPreset` with:
  - `id: 'insurance-meeting-prep'`
  - `name: 'Insurance Meeting Prep'`
  - `description: 'Adjuster meeting preparation with carrier intelligence, claim status, and filing strategy.'`
  - `audience: 'internal'`
  - `usageMoment: 'Meeting Prep'`
  - `icon: 'Shield'` (Lucide icon)
  - `modules`: 5 TopicModuleConfig entries:
    1. `insurance-status` -- enabled: true, required: true
    2. `carrier-intel` -- enabled: true, required: true (note: requiresWebSearch=true)
    3. `interaction-history` -- enabled: true, required: false
    4. `property-overview` -- enabled: true, required: false
    5. `next-steps` -- enabled: true, required: false

**competitive-edge.ts** -- Per INFOG-010:
- Export `const competitiveEdgePreset: InfographicPreset` with:
  - `id: 'competitive-edge'`
  - `name: 'Competitive Edge'`
  - `description: 'Competitive positioning analysis with market landscape and differentiation points.'`
  - `audience: 'internal'`
  - `usageMoment: 'Meeting Prep'`
  - `icon: 'Swords'` (Lucide icon)
  - `modules`: 5 TopicModuleConfig entries:
    1. `competitor-landscape` -- enabled: true, required: true
    2. `lead-scoring` -- enabled: true, required: false
    3. `property-overview` -- enabled: true, required: false
    4. `neighborhood-context` -- enabled: true, required: false
    5. `next-steps` -- enabled: true, required: false

**customer-leave-behind.ts** -- Per INFOG-011 (CRITICAL: customer-facing audience):
- Import `getBrandingForInfographic` from `../utils/brandingAssets` -- reference in description that light brand theme is applied automatically via this function
- Export `const customerLeaveBehindPreset: InfographicPreset` with:
  - `id: 'customer-leave-behind'`
  - `name: 'Customer Leave-Behind'`
  - `description: 'Professional customer-facing handout with property assessment, storm history, and company credentials. Light brand theme applied automatically.'`
  - `audience: 'customer-facing'` -- THIS triggers NB Pro or chain via Model Intelligence
  - `usageMoment: 'Leave-Behind'`
  - `icon: 'FileOutput'` (Lucide icon)
  - `modules`: 6 TopicModuleConfig entries:
    1. `property-overview` -- enabled: true, required: true
    2. `storm-timeline` -- enabled: true, required: false
    3. `insurance-status` -- enabled: true, required: false
    4. `next-steps` -- enabled: true, required: false
    5. `company-credentials` -- enabled: true, required: true
    6. `contact-info` -- enabled: true, required: true

The customer-leave-behind.ts file should include a JSDoc comment noting that the `customer-facing` audience value causes Model Intelligence to select NB Pro or an NB2-to-NB Pro chain for maximum visual fidelity. The template itself does NOT reference any model.
  </action>
  <verify>
    <automated>cd "C:/Users/scott/Documents/guardian-intel" && npx tsc --noEmit src/features/infographic-generator/templates/insurance-meeting-prep.ts src/features/infographic-generator/templates/competitive-edge.ts src/features/infographic-generator/templates/customer-leave-behind.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "insuranceMeetingPrepPreset" src/features/infographic-generator/templates/insurance-meeting-prep.ts
    - grep -q "Meeting Prep" src/features/infographic-generator/templates/insurance-meeting-prep.ts
    - grep -q "carrier-intel" src/features/infographic-generator/templates/insurance-meeting-prep.ts
    - grep -q "competitiveEdgePreset" src/features/infographic-generator/templates/competitive-edge.ts
    - grep -q "competitor-landscape" src/features/infographic-generator/templates/competitive-edge.ts
    - grep -q "customerLeaveBehindPreset" src/features/infographic-generator/templates/customer-leave-behind.ts
    - grep -q "customer-facing" src/features/infographic-generator/templates/customer-leave-behind.ts
    - grep -q "Leave-Behind" src/features/infographic-generator/templates/customer-leave-behind.ts
    - grep -q "company-credentials" src/features/infographic-generator/templates/customer-leave-behind.ts
    - grep -q "contact-info" src/features/infographic-generator/templates/customer-leave-behind.ts
  </acceptance_criteria>
  <done>Insurance Meeting Prep exports preset with 5 modules including web-search carrier-intel, internal audience, "Meeting Prep" moment. Competitive Edge exports preset with 5 modules, internal audience, "Meeting Prep" moment. Customer Leave-Behind exports preset with 6 modules, customer-facing audience, "Leave-Behind" moment. All compile with zero type errors. No model references in any template.</done>
</task>

</tasks>

<verification>
All 5 preset template files compile without errors:
```bash
cd "C:/Users/scott/Documents/guardian-intel" && npx tsc --noEmit src/features/infographic-generator/templates/*.ts
```

Each preset:
- Has unique id string
- References modules from AVAILABLE_MODULES (no inline TopicModule definitions)
- Specifies correct audience ('internal' or 'customer-facing')
- Specifies correct usageMoment
- Contains NO model names, quality tiers, or chain strategy references
</verification>

<success_criteria>
- 5 preset files exist in src/features/infographic-generator/templates/
- Pre-Knock: 6 modules, internal, "Parking Lot"
- Post-Storm: 5 modules (includes web search), internal, "Post-Storm"
- Insurance Prep: 5 modules (includes web search), internal, "Meeting Prep"
- Competitive Edge: 5 modules, internal, "Meeting Prep"
- Customer Leave-Behind: 6 modules, customer-facing, "Leave-Behind"
- All files compile with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-templates/03-01-SUMMARY.md`
</output>
